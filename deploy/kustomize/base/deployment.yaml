apiVersion: apps/v1
kind: Deployment
metadata:
  name: dynamic-config-webhook
  namespace: dynamic-config
  labels:
    app.kubernetes.io/name: dynamic-config
    app.kubernetes.io/component: webhook
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: dynamic-config
      app.kubernetes.io/component: webhook
  template:
    metadata:
      labels:
        app.kubernetes.io/name: dynamic-config
        app.kubernetes.io/component: webhook
    spec:
      serviceAccountName: dynamic-config-webhook
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: dynamic-config
              app.kubernetes.io/component: webhook
      containers:
        - name: webhook
          image: ghcr.io/dynamic-config-rs/dynamic-config-webhook:v0.2.0
          imagePullPolicy: IfNotPresent
          env:
            - name: DYNAMIC_CONFIG_AGENT_IMAGE
              value: ghcr.io/dynamic-config-rs/dynamic-config-agent:v0.2.0
            - name: DYNAMIC_CONFIG_WEBHOOK_ADDR
              value: "0.0.0.0:8443"
            # Where the plain-HTTP metrics port binds. Empty turns it off,
            # and the admission port still serves /metrics either way.
            - name: DYNAMIC_CONFIG_WEBHOOK_METRICS_ADDR
              value: "0.0.0.0:9091"
            # The installation, as the ConfigMap beside this rather than
            # as a grammar in the variables below. A variable still wins
            # where both say something.
            - name: DYNAMIC_CONFIG_WEBHOOK_DEFAULTS_FILE
              value: /etc/dynamic-config/installation.yaml
            - name: DYNAMIC_CONFIG_AGENT_CPU_REQUEST
              value: "10m"
            - name: DYNAMIC_CONFIG_AGENT_MEMORY_REQUEST
              value: "32Mi"
            - name: DYNAMIC_CONFIG_AGENT_MEMORY_LIMIT
              value: "64Mi"
            # Optional fleet defaults, per-store defaults and the gates —
            # set via an overlay patch; empty means unset, and the webhook
            # validates every value at startup:
            #   DYNAMIC_CONFIG_AGENT_FILE_MODE          "0640"
            #   DYNAMIC_CONFIG_AGENT_WATCH_SECONDS      "30"
            #   DYNAMIC_CONFIG_AGENT_MODE               "both"
            #   DYNAMIC_CONFIG_AGENT_VOLUME_MEDIUM      "disk"
            #   DYNAMIC_CONFIG_AGENT_NATIVE_SIDECAR     "true"
            #   DYNAMIC_CONFIG_AGENT_RUN_AS_USER        "1000"
            #   DYNAMIC_CONFIG_AGENT_RUN_AS_GROUP       "1000"
            #   DYNAMIC_CONFIG_AGENT_METRICS_PORT       "9102"
            #   DYNAMIC_CONFIG_AGENT_ENV                "HTTPS_PROXY=http://egress:3128"
            #   DYNAMIC_CONFIG_AGENT_SOURCE             "consul"   ("!" pins, "?" opens)
            #   DYNAMIC_CONFIG_AGENT_PATH               "/config/rendered.toml"
            #   DYNAMIC_CONFIG_AGENT_DEFAULTS_OVERRIDABLE "false"  (pins every set value)
            #   DYNAMIC_CONFIG_AGENT_STORE_DEFAULTS     "vault: overridable=false, endpoint=https://vault:8200, auth=kubernetes; s3: file-mode=0640?"
            #     ("!" pins a value, "?" opens one, overridable=false pins a store's group)
            #   DYNAMIC_CONFIG_WEBHOOK_AGENT_ENV_ALLOW  "payments: HTTPS_PROXY; *: RUST_LOG"
            #   DYNAMIC_CONFIG_WEBHOOK_SOURCE_ALLOW     "payments: vault, s3; *: consul"
            #   DYNAMIC_CONFIG_WEBHOOK_SOURCE_DENY      "sandbox: git"
          ports:
            - containerPort: 8443
              name: https
            # Metrics on a port of their own, in plain HTTP. The admission
            # port is mutual TLS against a CA the webhook may mint for
            # itself, so scraping *that* means handing Prometheus a client
            # certificate from it. The admission port still answers
            # /metrics, so an existing scrape keeps working.
            - containerPort: 9091
              name: metrics
          volumeMounts:
            - name: tls
              mountPath: /tls
              readOnly: true
            - name: installation
              mountPath: /etc/dynamic-config
              readOnly: true
          securityContext:
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
            seccompProfile:
              type: RuntimeDefault
          resources:
            requests: { cpu: 10m, memory: 32Mi }
            limits: { memory: 64Mi }
          readinessProbe:
            # /readyz, not /healthz: ready means a certificate this process
            # can serve with is loaded. In selfRotate mode that is false for
            # the first minute, and a replica reporting Ready without one is
            # a replica the Service sends admissions it cannot complete a
            # handshake for.
            httpGet: { path: /readyz, port: 8443, scheme: HTTPS }
            initialDelaySeconds: 2
          livenessProbe:
            httpGet: { path: /healthz, port: 8443, scheme: HTTPS }
            periodSeconds: 20
      volumes:
        - name: installation
          configMap:
            name: dynamic-config-webhook-installation
        - name: tls
          secret:
            secretName: dynamic-config-webhook-tls
